#include <BeanieBaby.h>

/**
 * Loads BeanieBaby data from provided url as a string.
 * @param url url to read data from.
 * @return string with url contents (i.e. json)
 */
std::string LoadBeanieBabyData(std::string url) {

}